import React from 'react';
import { View } from 'react-native';

// This is a placeholder component that could be expanded later to include
// more sophisticated questions during onboarding
const OnboardingQuestions = ({ onAnswer }) => {
  return <View />;
};

export default OnboardingQuestions;